# Generated configuration for netpilot.meadadigital.com
# Generated at: 2025-10-02T20:27:00.121298
# Domain ID: e9b58a3a-ce4f-449e-b7ea-260120fd13fc

# HTTP to HTTPS redirect
server {
    listen 80;
    server_name netpilot.meadadigital.com;
    return 301 https://$server_name$request_uri;
}

# Main server block
server {
    listen 443 ssl http2;
    server_name netpilot.meadadigital.com;


    # SSL configuration
    ssl_certificate /etc/ssl/certs/netpilot.meadadigital.com.crt;
    ssl_certificate_key /etc/ssl/private/netpilot.meadadigital.com.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Access and error logs
    access_log /var/log/nginx/netpilot.meadadigital.com_access.log;
    error_log /var/log/nginx/netpilot.meadadigital.com_error.log;

    # WWW redirect
    if ($host = 'www.netpilot.meadadigital.com') {
        return 301 $scheme://netpilot.meadadigital.com$request_uri;
    }

    # Proxy rule: /*
    # Priority: 2
    location /* {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    # Proxy rule: /api/*
    # Priority: 1
    location /api/* {
        proxy_pass http://backend:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

}